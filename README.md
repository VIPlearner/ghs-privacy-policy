# GHS Privacy Policy

Static website hosting the privacy policy for the GHS application.

## Local Development

Open `index.html` in your browser or run a local server:

```bash
python3 -m http.server 8000
```

## Deployment

Deploy to DigitalOcean App Platform by connecting this repository. It will automatically detect and serve as a static site.